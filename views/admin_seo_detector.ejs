<%- include('admin_layout_header', { title: 'Контроль Качества SEO', page: 'seo-detector' }) %>
<%
    let baseQueryParams = '';
    if (typeof currentRisk !== 'undefined' && currentRisk && currentRisk !== 'ALL' && !showDuplicates) baseQueryParams += '&risk=' + currentRisk;
    if (typeof showDuplicates !== 'undefined' && showDuplicates) baseQueryParams += '&duplicates=true';
    if (typeof currentPublishStatus !== 'undefined' && currentPublishStatus) baseQueryParams += '&publish_status=' + currentPublishStatus;
    
    // Для ссылок во вкладках (сохраняют publish_status, но меняют risk/duplicates)
    let tabsQueryParams = '';
    if (typeof currentPublishStatus !== 'undefined' && currentPublishStatus) tabsQueryParams += '&publish_status=' + currentPublishStatus;
    if (typeof currentSort !== 'undefined' && currentSort) tabsQueryParams += '&sort=' + currentSort;
    if (typeof currentOrder !== 'undefined' && currentOrder) tabsQueryParams += '&order=' + currentOrder;

    // Для ссылок во вкладках Publish (сохраняют risk/duplicates, меняют publish_status)
    let publishTabsQueryParams = '';
    if (typeof currentRisk !== 'undefined' && currentRisk && currentRisk !== 'ALL' && !showDuplicates) publishTabsQueryParams += '&risk=' + currentRisk;
    if (typeof showDuplicates !== 'undefined' && showDuplicates) publishTabsQueryParams += '&duplicates=true';
    if (typeof currentSort !== 'undefined' && currentSort) publishTabsQueryParams += '&sort=' + currentSort;
    if (typeof currentOrder !== 'undefined' && currentOrder) publishTabsQueryParams += '&order=' + currentOrder;

    // Функция для генерации ссылок сортировки
    function getSortLink(field) {
        let order = 'asc';
        if (typeof currentSort !== 'undefined' && currentSort === field && currentOrder === 'asc') {
            order = 'desc';
        }
        return `?sort=${field}&order=${order}${baseQueryParams}`;
    }

    // Для пагинации (нужны все параметры)
    let paginationParams = baseQueryParams;
    if (typeof currentSort !== 'undefined' && currentSort) paginationParams += '&sort=' + currentSort;
    if (typeof currentOrder !== 'undefined' && currentOrder) paginationParams += '&order=' + currentOrder;
%>
<details class="bg-slate-800 rounded-xl border border-slate-700 shadow-sm group" open>
    <summary class="p-4 cursor-pointer list-none flex justify-between items-center font-semibold text-white select-none [&::-webkit-details-marker]:hidden border-b border-slate-700 bg-slate-800/50 hover:bg-slate-700/50 transition-colors">
        <div class="flex items-center gap-2">
            <i data-lucide="list-checks" class="w-5 h-5 text-blue-400"></i>
            Качество карточек
        </div>
        <i data-lucide="chevron-down" class="w-5 h-5 text-slate-400 group-open:rotate-180 transition-transform"></i>
    </summary>
    <div class="flex flex-col h-[calc(100vh-142px)]">
    <div class="p-3 border-b border-slate-700 bg-slate-800/50">
        <div class="flex items-center justify-end gap-3 text-xs border-b border-slate-700/50 pb-2">
            <div class="flex items-center gap-1.5">
                <span class="text-slate-400">Скан:</span>
                <span class="font-medium text-white"><%= scanStats.scanned %>/<%= scanStats.total %></span>
                <% if (scanStats.scanned < scanStats.total) { %><i data-lucide="loader-2" class="w-3 h-3 text-blue-400 animate-spin"></i><% } else { %><i data-lucide="check-circle" class="w-3 h-3 text-green-400"></i><% } %>
            </div>
            <% if (scanStats.lastScanTime) { %><div class="text-slate-500 border-l border-slate-700 pl-3"><%= scanStats.lastScanTime %></div><% } %>
        </div>

        <div class="mt-3 flex flex-col lg:flex-row justify-between gap-3 lg:items-center">
            <!-- Вкладки Risk -->
            <div class="flex gap-1.5 flex-wrap w-full lg:w-auto">
                <a href="/admin/seo-detector?risk=ALL<%= tabsQueryParams %>" class="px-3 py-2 md:py-1.5 text-xs rounded-lg font-medium <%= currentRisk === 'ALL' && !showDuplicates ? 'bg-blue-600 text-white' : 'bg-slate-700 text-slate-300 hover:bg-slate-600' %> transition-colors flex-1 lg:flex-none text-center">
                    Все (<%= stats.total %>)
                </a>
                <a href="?risk=SAFE<%= tabsQueryParams %>" class="px-3 py-2 md:py-1.5 text-xs rounded-lg font-medium <%= currentRisk === 'SAFE' && !showDuplicates ? 'bg-green-600 text-white' : 'bg-slate-700 text-slate-300 hover:bg-slate-600' %> transition-colors flex-1 lg:flex-none text-center">
                    Удачные (<%= stats.safe %>)
                </a>
                <a href="?risk=WARNING<%= tabsQueryParams %>" class="px-3 py-2 md:py-1.5 text-xs rounded-lg font-medium <%= currentRisk === 'WARNING' && !showDuplicates ? 'bg-yellow-600 text-white' : 'bg-slate-700 text-slate-300 hover:bg-slate-600' %> transition-colors flex-1 lg:flex-none text-center">
                    Неудачные (<%= stats.warning %>)
                </a>
                <a href="?duplicates=true<%= tabsQueryParams %>" class="px-3 py-2 md:py-1.5 text-xs rounded-lg font-medium <%= showDuplicates ? 'bg-purple-600 text-white' : 'bg-slate-700 text-slate-300 hover:bg-slate-600' %> transition-colors flex-1 lg:flex-none text-center">
                    Дубликаты (<%= duplicatesCount %>)
                </a>
            </div>

            <!-- Вкладки Publish Status -->
            <div class="flex gap-1.5 flex-wrap w-full lg:w-auto">
                <div class="hidden lg:block w-px h-7 bg-slate-700 mx-1"></div>
                <a href="?publish_status=<%= publishTabsQueryParams %>" class="px-3 py-2 md:py-1.5 text-xs rounded-lg font-medium <%= (!currentPublishStatus) ? 'bg-slate-600 text-white shadow-inner' : 'bg-slate-700 text-slate-300 hover:bg-slate-600' %> transition-colors flex-1 lg:flex-none text-center">
                    Все статусы (<%= publishStats.total %>)
                </a>
                <a href="?publish_status=PUBLISHED<%= publishTabsQueryParams %>" class="px-3 py-2 md:py-1.5 text-xs rounded-lg font-medium <%= (currentPublishStatus === 'PUBLISHED') ? 'bg-blue-600 text-white shadow-inner' : 'bg-slate-700 text-slate-300 hover:bg-slate-600' %> transition-colors flex-1 lg:flex-none text-center">
                    Опубликовано (<%= publishStats.published %>)
                </a>
                <a href="?publish_status=UNPUBLISHED<%= publishTabsQueryParams %>" class="px-3 py-2 md:py-1.5 text-xs rounded-lg font-medium <%= (currentPublishStatus === 'UNPUBLISHED') ? 'bg-purple-600 text-white shadow-inner' : 'bg-slate-700 text-slate-300 hover:bg-slate-600' %> transition-colors flex-1 lg:flex-none text-center">
                    Отложено (<%= publishStats.unpublished %>)
                </a>
            </div>
        </div>
    </div>

    <div class="overflow-auto flex-1 p-0 min-h-0">
        <table class="w-full text-left border-collapse min-w-[700px]">
            <thead class="sticky top-0 bg-slate-900 border-b border-slate-700 z-10">
                <tr class="text-slate-300 text-xs">
                    <th class="px-2 py-2.5 font-medium text-left">
                        <a href="<%= getSortLink('brand') %>" class="inline-flex items-center gap-1 hover:text-white transition-colors">
                            Карточка
                            <% if (typeof currentSort !== 'undefined' && currentSort === 'brand') { %><i data-lucide="<%= currentOrder === 'desc' ? 'arrow-down' : 'arrow-up' %>" class="w-3 h-3 text-blue-400"></i><% } else { %><i data-lucide="arrow-up-down" class="w-3 h-3 opacity-30"></i><% } %>
                        </a>
                    </th>
                    <th class="px-2 py-2.5 font-medium text-center">
                        <a href="<%= getSortLink('seoScore') %>" class="inline-flex items-center gap-1 hover:text-white transition-colors">
                            Оценка СЕО
                            <% if (typeof currentSort !== 'undefined' && currentSort === 'seoScore') { %><i data-lucide="<%= currentOrder === 'desc' ? 'arrow-down' : 'arrow-up' %>" class="w-3 h-3 text-blue-400"></i><% } else { %><i data-lucide="arrow-up-down" class="w-3 h-3 opacity-30"></i><% } %>
                        </a>
                    </th>
                    <th class="px-2 py-2.5 font-medium text-center">
                        <a href="<%= getSortLink('uniquenessScore') %>" class="inline-flex items-center gap-1 hover:text-white transition-colors">
                            Текст
                            <% if (typeof currentSort !== 'undefined' && currentSort === 'uniquenessScore') { %><i data-lucide="<%= currentOrder === 'desc' ? 'arrow-down' : 'arrow-up' %>" class="w-3 h-3 text-blue-400"></i><% } else { %><i data-lucide="arrow-up-down" class="w-3 h-3 opacity-30"></i><% } %>
                        </a>
                    </th>
                    <th class="px-2 py-2.5 font-medium text-center w-28">
                        <a href="<%= getSortLink('seoRisk') %>" class="inline-flex items-center justify-center gap-1 hover:text-white transition-colors">
                            Статус
                            <% if (typeof currentSort !== 'undefined' && currentSort === 'seoRisk') { %><i data-lucide="<%= currentOrder === 'desc' ? 'arrow-down' : 'arrow-up' %>" class="w-3 h-3 text-blue-400"></i><% } else { %><i data-lucide="arrow-up-down" class="w-3 h-3 opacity-30"></i><% } %>
                        </a>
                    </th>
                    <th class="px-2 py-2.5 font-medium text-center w-32">
                        <a href="<%= getSortLink('created_at') %>" class="inline-flex items-center justify-center gap-1 hover:text-white transition-colors">
                            Публикация
                            <% if (typeof currentSort !== 'undefined' && currentSort === 'created_at') { %><i data-lucide="<%= currentOrder === 'desc' ? 'arrow-down' : 'arrow-up' %>" class="w-3 h-3 text-blue-400"></i><% } else { %><i data-lucide="arrow-up-down" class="w-3 h-3 opacity-30"></i><% } %>
                        </a>
                    </th>
                    <th class="px-2 py-2.5 font-medium text-right w-36">Действия</th>
                </tr>
            </thead>
            <tbody class="text-sm divide-y divide-slate-700/50">
                <% reports.forEach(report => { %>
                    <tr class="hover:bg-slate-700/30 transition-colors">
                        <td class="px-2 py-2">
                            <a href="/catalog/<%= report.brand.toLowerCase() %>/<%= report.model.toLowerCase() %>/<%= report.code.toLowerCase() %>" target="_blank" class="hover:underline flex items-center gap-1.5 w-fit">
                                <span class="font-medium text-blue-400 text-xs sm:text-sm whitespace-nowrap"><%= report.brand.charAt(0).toUpperCase() + report.brand.slice(1).toLowerCase() %> <%= report.model.charAt(0).toUpperCase() + report.model.slice(1).toLowerCase() %></span>
                                <span class="text-slate-300 font-mono font-bold bg-slate-700/50 border border-slate-600 px-1 rounded text-[10px] sm:text-xs"><%= report.code.toUpperCase() %></span>
                            </a>
                        </td>
                        <td class="px-2 py-2 text-center">
                            <% 
                                let scoreColor = 'text-green-400';
                                if(report.seoRisk === 'WARNING') scoreColor = 'text-yellow-400';
                                if(report.seoRisk === 'DANGER') scoreColor = 'text-red-400';
                            %>
                            <span class="text-sm sm:text-base font-bold <%= scoreColor %>"><%= report.seoScore %></span>
                        </td>
                        <td class="px-2 py-2 text-center">
                            <% 
                                let uScore = report.uniquenessScore || 0;
                                let uColor = 'text-green-400 bg-green-500/10 border-green-500/30';
                                if(uScore < 50) uColor = 'text-yellow-400 bg-yellow-500/10 border-yellow-500/30';
                                if(uScore < 30) uColor = 'text-red-400 bg-red-500/10 border-red-500/30 font-bold';
                            %>
                            <span class="inline-block px-1.5 py-0.5 rounded border text-[10px] sm:text-xs <%= uColor %>">
                                <%= uScore %>%
                            </span>
                        </td>
                        <td class="px-2 py-2 text-center">
                            <% if (report.seoRisk === 'SAFE') { %>
                                <span class="bg-green-500/10 text-green-400 text-[10px] px-1.5 py-0.5 rounded border border-green-500/30 font-medium">Успешно</span>
                            <% } else if (report.seoRisk === 'WARNING') { %>
                                <span class="bg-yellow-500/10 text-yellow-400 text-[10px] px-1.5 py-0.5 rounded border border-yellow-500/30 font-medium">Внимание</span>
                            <% } else { %>
                                <span class="bg-red-500/10 text-red-400 text-[10px] px-1.5 py-0.5 rounded border border-red-500/30 font-medium">Критично</span>
                            <% } %>
                        </td>
                        <td class="px-2 py-2 text-center">
                            <% 
                                const isPublished = new Date(report.created_at) <= new Date();
                                const isFarFuture = new Date(report.created_at).getFullYear() === 2099;
                            %>
                            <% if (isPublished) { %>
                                <span class="text-blue-400 text-[10px] leading-tight flex flex-col items-center">
                                    <span>Опубликовано</span>
                                    <span class="opacity-70"><%= new Date(report.created_at).toLocaleDateString('ru-RU') %></span>
                                </span>
                            <% } else if (isFarFuture) { %>
                                <span class="text-slate-400 text-[10px] flex flex-col items-center">Снято</span>
                            <% } else { %>
                                <span class="text-purple-400 text-[10px] leading-tight flex flex-col items-center">
                                    <span>Отложено</span>
                                    <span class="opacity-70"><%= new Date(report.created_at).toLocaleDateString('ru-RU') %></span>
                                </span>
                            <% } %>
                        </td>
                        <td class="px-2 py-2 text-right">
                            <div class="flex items-center justify-end gap-1">
                                <% const isEnriched = !!report.tools_table_md || (report.seoScore === 95 && report.uniquenessScore === 100); %>
                                <% if (isEnriched) { %>
                                    <button disabled title="Уже обогащено" class="w-10 h-10 md:w-8 md:h-8 flex items-center justify-center text-emerald-400 cursor-default rounded">
                                        <i data-lucide="check-circle" class="w-4 h-4"></i>
                                    </button>
                                <% } else { %>
                                    <button id="enrich-btn-<%= report.id %>" onclick="enrichCard('<%= report.id %>')" title="СЕО Обогащение (ИИ)" class="w-10 h-10 md:w-8 md:h-8 flex items-center justify-center text-slate-400 hover:text-emerald-400 hover:bg-slate-700 rounded transition-colors">
                                        <i data-lucide="sparkles" id="enrich-icon-<%= report.id %>" class="w-4 h-4"></i>
                                    </button>
                                <% } %>
                                <% const hasRetroLinks = (report.full_analysis_markdown || report.summary || '').includes('ПДД и полезные ресурсы'); %>
                                <button class="btn-enrich-single w-10 h-10 md:w-8 md:h-8 flex items-center justify-center rounded transition-colors <%= hasRetroLinks ? 'text-emerald-400 hover:text-emerald-300' : 'text-rose-400 hover:text-rose-300 hover:bg-slate-700' %>" data-id="<%= report.id %>" title="SEO Обогащение (Словари)">
                                    <i data-lucide="link" class="w-4 h-4"></i>
                                </button>
                                <% if (isPublished) { %>
                                    <button onclick="unpublishCard('<%= report.id %>')" title="Снять с публикации" class="w-10 h-10 md:w-8 md:h-8 flex items-center justify-center text-slate-400 hover:text-yellow-400 hover:bg-slate-700 rounded transition-colors">
                                        <i data-lucide="clock" class="w-4 h-4"></i>
                                    </button>
                                <% } else { %>
                                    <button onclick="publishCard('<%= report.id %>')" title="Опубликовать сейчас" class="w-10 h-10 md:w-8 md:h-8 flex items-center justify-center text-slate-400 hover:text-green-400 hover:bg-slate-700 rounded transition-colors">
                                        <i data-lucide="check-circle" class="w-4 h-4"></i>
                                    </button>
                                <% } %>
                                <button onclick='openEditModal(<%- JSON.stringify(report).replace(/'/g, "&apos;") %>)' title="Редактировать" class="w-10 h-10 md:w-8 md:h-8 flex items-center justify-center text-slate-400 hover:text-blue-400 hover:bg-slate-700 rounded transition-colors">
                                    <i data-lucide="edit-2" class="w-4 h-4"></i>
                                </button>
                                <button onclick="deleteCard('<%= report.id %>')" title="Удалить" class="w-10 h-10 md:w-8 md:h-8 flex items-center justify-center text-slate-400 hover:text-red-400 hover:bg-slate-700 rounded transition-colors">
                                    <i data-lucide="trash-2" class="w-4 h-4"></i>
                                </button>
                            </div>
                        </td>
                    </tr>
                <% }) %>
            </tbody>
        </table>
    </div>

    <!-- Пагинация -->
    <div class="p-4 border-t border-slate-700 bg-slate-800/50 flex flex-col md:flex-row justify-between items-center gap-4">
        <div class="text-sm text-slate-400">
            Всего карточек: <strong><%= currentRisk === 'ALL' ? stats.total : (currentRisk === 'SAFE' ? stats.safe : (currentRisk === 'WARNING' ? stats.warning : stats.danger)) %></strong>
        </div>
        
        <div class="flex items-center gap-1">
            <!-- Кнопки Назад -->
            <% if (page > 1) { %>
                <a href="?page=1<%= paginationParams %>" class="p-1.5 bg-slate-700 text-slate-300 rounded hover:text-white hover:bg-slate-600 transition-colors" title="Первая страница">
                    <i data-lucide="chevrons-left" class="w-4 h-4"></i>
                </a>
                <a href="?page=<%= page - 1 %><%= paginationParams %>" class="p-1.5 bg-slate-700 text-slate-300 rounded hover:text-white hover:bg-slate-600 transition-colors" title="Предыдущая">
                    <i data-lucide="chevron-left" class="w-4 h-4"></i>
                </a>
            <% } else { %>
                <span class="p-1.5 bg-slate-800 text-slate-600 rounded cursor-not-allowed">
                    <i data-lucide="chevrons-left" class="w-4 h-4"></i>
                </span>
                <span class="p-1.5 bg-slate-800 text-slate-600 rounded cursor-not-allowed">
                    <i data-lucide="chevron-left" class="w-4 h-4"></i>
                </span>
            <% } %>

            <!-- Поле ввода -->
            <form method="GET" action="/admin/seo-detector" class="flex items-center mx-2 bg-slate-900/50 rounded border border-slate-700">
                <% if (typeof currentRisk !== 'undefined' && currentRisk && currentRisk !== 'ALL' && !showDuplicates) { %><input type="hidden" name="risk" value="<%= currentRisk %>"><% } %>
                <% if (typeof showDuplicates !== 'undefined' && showDuplicates) { %><input type="hidden" name="duplicates" value="true"><% } %>
                <% if (typeof currentPublishStatus !== 'undefined' && currentPublishStatus) { %><input type="hidden" name="publish_status" value="<%= currentPublishStatus %>"><% } %>
                <% if (typeof currentSort !== 'undefined' && currentSort) { %><input type="hidden" name="sort" value="<%= currentSort %>"><% } %>
                <% if (typeof currentOrder !== 'undefined' && currentOrder) { %><input type="hidden" name="order" value="<%= currentOrder %>"><% } %>
                
                <input type="number" name="page" value="<%= page %>" min="1" max="<%= totalPages %>" class="w-12 px-1 py-1 text-center bg-transparent text-white text-sm outline-none no-spinners">
                <span class="text-slate-500 text-sm px-2 border-l border-slate-700">из <%= totalPages %></span>
            </form>

            <!-- Кнопки Вперед -->
            <% if (page < totalPages) { %>
                <a href="?page=<%= page + 1 %><%= paginationParams %>" class="p-1.5 bg-slate-700 text-slate-300 rounded hover:text-white hover:bg-slate-600 transition-colors" title="Следующая">
                    <i data-lucide="chevron-right" class="w-4 h-4"></i>
                </a>
                <a href="?page=<%= totalPages %><%= paginationParams %>" class="p-1.5 bg-slate-700 text-slate-300 rounded hover:text-white hover:bg-slate-600 transition-colors" title="Последняя страница">
                    <i data-lucide="chevrons-right" class="w-4 h-4"></i>
                </a>
            <% } else { %>
                <span class="p-1.5 bg-slate-800 text-slate-600 rounded cursor-not-allowed">
                    <i data-lucide="chevron-right" class="w-4 h-4"></i>
                </span>
                <span class="p-1.5 bg-slate-800 text-slate-600 rounded cursor-not-allowed">
                    <i data-lucide="chevrons-right" class="w-4 h-4"></i>
                </span>
            <% } %>
        </div>
    </div>
    </div>
</details>

<details class="bg-slate-800 rounded-xl border border-slate-700 shadow-sm group mt-4" id="broken-links">
    <summary class="p-4 cursor-pointer list-none flex justify-between items-center font-semibold text-white select-none [&::-webkit-details-marker]:hidden border-b border-slate-700 bg-slate-800/50 hover:bg-slate-700/50 transition-colors">
        <div class="flex items-center gap-2">
            <i data-lucide="link-2-off" class="w-5 h-5 text-red-400"></i>
            Сироты, Проблемы верстки и Битые ссылки
        </div>
        <div class="flex items-center gap-3">
            <span class="text-xs font-medium bg-slate-700 px-2 py-1 rounded-md text-slate-300"><%= brokenLinks.length %> шт.</span>
            <i data-lucide="chevron-down" class="w-5 h-5 text-slate-400 group-open:rotate-180 transition-transform"></i>
        </div>
    </summary>
    <div class="flex flex-col max-h-[500px]">
        <div class="flex-1 overflow-auto bg-[#0f172a]/50 p-4">
            <% if (brokenLinks.length === 0) { %>
                <div class="flex flex-col items-center justify-center h-full text-slate-500 gap-3">
                    <i data-lucide="check-circle" class="w-12 h-12 text-green-500/50"></i>
                    <p>Отлично! Проблем верстки и битых ссылок не найдено.</p>
                </div>
            <% } else { %>
                <div class="overflow-x-auto min-h-0">
                    <table class="w-full text-left border-collapse min-w-[700px]">
                        <thead class="sticky top-0 bg-slate-900 border-b border-slate-700 z-10">
                            <tr class="text-slate-300 text-xs">
                                <th class="px-2 py-2.5 font-medium">Статья (Где найдено)</th>
                                <th class="px-2 py-2.5 font-medium">Анкор (Текст)</th>
                                <th class="px-2 py-2.5 font-medium">Битая ссылка</th>
                                <th class="px-2 py-2.5 font-medium text-right">Действия</th>
                            </tr>
                        </thead>
                        <tbody class="text-sm divide-y divide-slate-700/50">
                            <% brokenLinks.forEach((link, idx) => { %>
                                <tr class="hover:bg-slate-700/30 transition-colors" id="row-<%= idx %>">
                                    <td class="px-2 py-2">
                                        <div class="text-white font-medium text-sm flex items-center gap-2 flex-wrap">
                                            <%= link.title %>
                                            <% if (link.url === 'СТРАНИЦА-СИРОТА') { %>
                                                <span class="bg-purple-500/10 border border-purple-500/20 text-purple-400 text-[10px] px-1.5 py-0.5 rounded whitespace-nowrap">Сирота</span>
                                            <% } else if (['ОШИБКА ВЕРСТКИ', 'ОБОРВАННЫЙ ТЕКСТ', 'АРТЕФАКТ'].includes(link.url)) { %>
                                                <span class="bg-orange-500/10 border border-orange-500/20 text-orange-400 text-[10px] px-1.5 py-0.5 rounded whitespace-nowrap">Проблемная верстка</span>
                                            <% } else { %>
                                                <span class="bg-red-500/10 border border-red-500/20 text-red-400 text-[10px] px-1.5 py-0.5 rounded whitespace-nowrap">Битая ссылка</span>
                                            <% } %>
                                        </div>
                                        <div class="text-xs text-slate-500 mt-0.5">Поле: <%= link.field %></div>
                                    </td>
                                    <td class="px-2 py-2 text-slate-300">
                                        <span class="bg-slate-800 border border-slate-700 px-1.5 py-0.5 rounded text-xs">"<%= link.text %>"</span>
                                    </td>
                                    <td class="px-2 py-2">
                                        <div class="text-red-400 font-mono text-[11px] break-all max-w-[300px]"><%= link.url %></div>
                                    </td>
                                    <td class="px-2 py-2 text-right">
                                        <div class="flex justify-end gap-1">
                                            <button onclick="editCardFromLinks('<%= link.id %>')" title="Редактировать" class="w-8 h-8 flex items-center justify-center text-slate-400 hover:text-blue-400 hover:bg-slate-700 rounded transition-colors">
                                                <i data-lucide="edit-2" class="w-4 h-4"></i>
                                            </button>
                                            <button onclick="deleteCard('<%= link.id %>')" title="Удалить карточку" class="w-8 h-8 flex items-center justify-center text-slate-400 hover:text-red-400 hover:bg-slate-700 rounded transition-colors">
                                                <i data-lucide="trash-2" class="w-4 h-4"></i>
                                            </button>
                                            <button onclick="removeBrokenLink('<%= link.id %>', '<%= link.url.replace(/'/g, "\\'") %>', <%= idx %>)" title="Скрыть уведомление" class="w-8 h-8 flex items-center justify-center text-slate-400 hover:text-yellow-400 hover:bg-slate-700 rounded transition-colors">
                                                <i data-lucide="x" class="w-4 h-4"></i>
                                            </button>
                                        </div>
                                    </td>
                                </tr>
                            <% }) %>
                        </tbody>
                    </table>
                </div>
            <% } %>
        </div>
    </div>
</details>

<script>
async function removeBrokenLink(reportId, brokenUrl, rowIndex) {
    if(!confirm('Автоматически удалить эту ссылку из текста статьи?')) return;
    
    try {
        const res = await fetch('/admin/api/links/remove-404', {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify({ id: reportId, url: brokenUrl })
        });
        const data = await res.json();
        if(data.success) {
            document.getElementById('row-' + rowIndex).style.display = 'none';
        } else {
            alert('Ошибка: ' + data.error);
        }
    } catch(e) {
        alert('Сетевая ошибка');
    }
}
</script>

<!-- Модальное окно редактирования -->
<div id="editModal" class="fixed inset-0 bg-black/60 hidden items-center justify-center z-50 backdrop-blur-sm p-4">
    <div class="bg-slate-800 rounded-xl border border-slate-700 w-full max-w-4xl shadow-2xl flex flex-col h-[90vh]">
        <div class="p-4 border-b border-slate-700 flex justify-between items-center bg-slate-900/50 rounded-t-xl">
            <h3 class="text-lg font-bold text-white flex items-center gap-2">
                <i data-lucide="edit-3" class="w-5 h-5 text-blue-400"></i>
                Редактор SEO контента <span id="editModalTitle" class="text-blue-400 ml-2"></span>
            </h3>
            <button onclick="closeEditModal()" class="text-slate-400 hover:text-white transition-colors">
                <i data-lucide="x" class="w-5 h-5"></i>
            </button>
        </div>
        
        <div class="p-6 overflow-y-auto flex-1 flex flex-col lg:flex-row gap-6">
            <!-- Левая колонка: Детализация оценки -->
            <div class="w-full lg:w-1/3 bg-slate-900/50 rounded-xl border border-slate-700 p-4 self-start sticky top-0">
                <h4 class="text-white font-semibold mb-3 flex items-center gap-2">
                    <i data-lucide="bar-chart-2" class="w-4 h-4 text-blue-400"></i> Детализация оценки
                </h4>
                <div id="scoreDetailsContainer" class="space-y-3">
                    <!-- Заполняется через JS -->
                </div>
            </div>

            <!-- Правая колонка: Форма -->
            <div class="w-full lg:w-2/3 space-y-4">
                <input type="hidden" id="edit_id">
                
                <div>
                    <label class="block text-xs font-medium text-slate-400 mb-1">SEO Title (40-70 симв.)</label>
                    <input type="text" id="edit_seoTitle" class="w-full bg-slate-900 border border-slate-700 rounded-lg px-3 py-2 text-white text-sm focus:outline-none focus:border-blue-500">
                </div>
                
                <div>
                    <label class="block text-xs font-medium text-slate-400 mb-1">SEO Description (120-160 симв.)</label>
                    <textarea id="edit_seoDescription" rows="2" class="w-full bg-slate-900 border border-slate-700 rounded-lg px-3 py-2 text-white text-sm focus:outline-none focus:border-blue-500"></textarea>
                </div>
                
                <div>
                    <label class="block text-xs font-medium text-slate-400 mb-1">Teaser Text (Вводный абзац)</label>
                    <textarea id="edit_teaserText" rows="3" class="w-full bg-slate-900 border border-slate-700 rounded-lg px-3 py-2 text-white text-sm focus:outline-none focus:border-blue-500"></textarea>
                </div>

                <div>
                    <label class="block text-xs font-medium text-slate-400 mb-1">Основной контент (Markdown, используйте ## и списки)</label>
                    <textarea id="edit_fullAnalysis" rows="10" class="w-full bg-slate-900 border border-slate-700 rounded-lg px-3 py-2 text-white text-sm focus:outline-none focus:border-blue-500 font-mono"></textarea>
                </div>
            </div>
        </div>

        <div class="p-4 border-t border-slate-700 bg-slate-900/50 flex justify-end gap-3 rounded-b-xl">
            <button onclick="closeEditModal()" class="px-4 py-2 bg-slate-700 hover:bg-slate-600 text-white rounded-lg text-sm transition-colors">Отмена</button>
            <button onclick="saveSeoData()" id="saveBtn" class="px-4 py-2 bg-blue-600 hover:bg-blue-500 text-white rounded-lg text-sm font-medium transition-colors flex items-center gap-2">
                <i data-lucide="save" class="w-4 h-4"></i> Сохранить и Пересчитать
            </button>
        </div>
    </div>
</div>

<script>
    const modal = document.getElementById('editModal');

    async function openEditModal(report) {
        document.getElementById('edit_id').value = report.id;
        document.getElementById('editModalTitle').innerText = "Редактирование карточки";
        document.getElementById('edit_seoTitle').value = report.seoTitle || '';
        document.getElementById('edit_seoDescription').value = report.seoDescription || '';
        document.getElementById('edit_teaserText').value = report.teaser_text || '';
        document.getElementById('edit_fullAnalysis').value = report.full_analysis_markdown || '';
        
        // Показываем модалку сразу
        modal.classList.remove('hidden');
        modal.classList.add('flex');
        
        const detailsContainer = document.getElementById('scoreDetailsContainer');
        detailsContainer.innerHTML = '<div class="text-slate-400 text-sm flex items-center gap-2 py-4"><i data-lucide="loader-2" class="w-4 h-4 animate-spin"></i> Вычисление дубликатов и разбор баллов...</div>';
        lucide.createIcons();

        try {
            // Асинхронно запрашиваем детализацию (с тяжелым поиском дубликатов)
            const res = await fetch('/admin/api/seo-detector/details/' + report.id);
            const data = await res.json();
            
            detailsContainer.innerHTML = '';
            
            if (data.success && data.details && data.details.length > 0) {
                data.details.forEach(detail => {
                    let icon = 'check-circle';
                    let colorClass = 'text-green-400';
                    let bgClass = 'bg-green-500/10 border-green-500/20';
                    
                    if (detail.status === 'warning') {
                        icon = 'alert-triangle';
                        colorClass = 'text-yellow-400';
                        bgClass = 'bg-yellow-500/10 border-yellow-500/20';
                    } else if (detail.status === 'danger') {
                        icon = 'x-circle';
                        colorClass = 'text-red-400';
                        bgClass = 'bg-red-500/10 border-red-500/20';
                    }

                    detailsContainer.innerHTML += `
                        <div class="p-3 rounded-lg border ${bgClass}">
                            <div class="flex justify-between items-start mb-1">
                                <span class="font-medium text-sm ${colorClass} flex items-center gap-1.5">
                                    <i data-lucide="${icon}" class="w-4 h-4"></i> ${detail.label}
                                </span>
                                <span class="text-xs font-bold text-white bg-slate-800 px-1.5 py-0.5 rounded">${detail.score}/${detail.max}</span>
                            </div>
                            <p class="text-xs text-slate-300 leading-relaxed">${detail.text}</p>
                        </div>
                    `;
                });
            } else {
                detailsContainer.innerHTML = '<div class="text-slate-400 text-sm py-4">Нет данных для отображения.</div>';
            }
        } catch (e) {
            detailsContainer.innerHTML = '<div class="text-red-400 text-sm py-4">Ошибка загрузки детализации.</div>';
        }
        
        lucide.createIcons();
    }

    function closeEditModal() {
        modal.classList.add('hidden');
        modal.classList.remove('flex');
    }

    async function saveSeoData() {
        const id = document.getElementById('edit_id').value;
        const btn = document.getElementById('saveBtn');
        const originalText = btn.innerHTML;
        
        btn.innerHTML = '<i data-lucide="loader-2" class="w-4 h-4 animate-spin"></i> Сохранение...';
        btn.disabled = true;
        lucide.createIcons();

        try {
            const res = await fetch('/admin/api/seo-detector/update', {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify({
                    id: id,
                    seoTitle: document.getElementById('edit_seoTitle').value,
                    seoDescription: document.getElementById('edit_seoDescription').value,
                    teaser_text: document.getElementById('edit_teaserText').value,
                    full_analysis_markdown: document.getElementById('edit_fullAnalysis').value
                })
            });

            const data = await res.json();
            if (data.success) {
                // Перезагрузка страницы для обновления таблицы
                window.location.reload();
            } else {
                alert(data.error || 'Ошибка сохранения');
            }
        } catch (e) {
            alert('Сетевая ошибка');
        } finally {
            btn.innerHTML = originalText;
            btn.disabled = false;
            lucide.createIcons();
        }
    }

    async function publishCard(id) {
            if (!confirm('Опубликовать эту карточку прямо сейчас?')) return;
            try {
                const res = await fetch('/admin/api/seo-detector/publish', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({ id })
                });
                if (res.ok) window.location.reload();
                else alert('Ошибка публикации');
            } catch(e) { alert('Сетевая ошибка'); }
        }

        async function unpublishCard(id) {
            if (!confirm('Снять карточку с публикации? Она станет недоступна для пользователей.')) return;
            try {
                const res = await fetch('/admin/api/seo-detector/unpublish', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({ id })
                });
                if (res.ok) window.location.reload();
                else alert('Ошибка снятия с публикации');
            } catch(e) { alert('Сетевая ошибка'); }
        }

        async function deleteCard(id) {
            if (!confirm('Удалить эту карточку навсегда? Это действие необратимо!')) return;
            try {
                const res = await fetch('/admin/api/seo-detector/delete', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({ id })
                });
                if (res.ok) window.location.reload();
                else alert('Ошибка удаления');
            } catch(e) { alert('Сетевая ошибка'); }
        }

        async function enrichCard(id) {
            const btn = document.getElementById('enrich-btn-' + id);
            const icon = document.getElementById('enrich-icon-' + id);
            
            // Запускаем лоадер
            btn.disabled = true;
            btn.classList.remove('hover:text-emerald-400', 'hover:bg-slate-700');
            icon.setAttribute('data-lucide', 'loader-2');
            icon.classList.add('animate-spin', 'text-blue-400');
            lucide.createIcons();

            try {
                const res = await fetch('/admin/api/seo/enrich-single', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({ id })
                });
                
                const data = await res.json();
                
                if (data.success) {
                    // Перезагружаем страницу чтобы обновились баллы и таблицы
                    window.location.reload();
                } else {
                    throw new Error(data.error);
                }
            } catch(e) { 
                // Оранжевый крестик (ошибка)
                icon.classList.remove('animate-spin', 'text-blue-400');
                icon.setAttribute('data-lucide', 'x-circle');
                icon.classList.add('text-orange-400');
                lucide.createIcons();
                alert('Ошибка обогащения: ' + e.message);
            }
        }

        document.querySelectorAll('.btn-enrich-single').forEach(btn => {
            btn.addEventListener('click', async function() {
                const id = this.getAttribute('data-id');
                const originalHtml = this.innerHTML;
                
                this.disabled = true;
                this.innerHTML = '<i data-lucide="loader-2" class="w-4 h-4 animate-spin text-cyan-400"></i>';
                lucide.createIcons();

                try {
                    const res = await fetch(`/admin/api/seo-enrich/single/${id}`, { method: 'POST' });
                    const data = await res.json();
                    
                    if (data.success || data.status === 'already_enriched') {
                        this.innerHTML = '<i data-lucide="link" class="w-4 h-4"></i>';
                        this.classList.remove('text-rose-400', 'hover:text-rose-300', 'hover:bg-slate-700');
                        this.classList.add('text-emerald-400', 'hover:text-emerald-300');
                        lucide.createIcons();
                        if (data.status === 'already_enriched') {
                            this.title = 'Уже обогащено';
                        }
                        if (data.recreated) {
                            window.location.reload();
                        }
                    } else {
                        throw new Error(data.error || 'Unknown error');
                    }
                } catch(e) {
                    this.innerHTML = '<i data-lucide="x-circle" class="w-4 h-4 text-rose-400"></i>';
                    lucide.createIcons();
                    alert('Ошибка обогащения: ' + e.message);
                }
            });
        });

        async function editCardFromLinks(id) {
            try {
                const res = await fetch(`/admin/api/seo-detector/report/${id}`);
                const data = await res.json();
                if (data.success && data.report) {
                    openEditModal(data.report);
                } else {
                    alert('Не удалось загрузить данные карточки');
                }
            } catch (e) {
                console.error(e);
                alert('Ошибка сети при загрузке карточки');
            }
        }
    </script>

<%- include('admin_layout_footer') %>
